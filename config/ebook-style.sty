\ProvidesPackage{ebook-style}
%\usepackage{showframe} % display box borders for debugging

\usepackage[T1]{fontenc} % use 8bit font encoding for proper hyphenation etc.
\usepackage{titlepic}

% for /autoref
\usepackage{hyperref,xcolor}
% remove ugly borders around clickable links (make links darker instead)
\hypersetup{
    colorlinks,
    linkcolor={red!40!black},
    citecolor={blue!30!black},
    urlcolor={blue!50!black}
}

% set font
\usepackage{fontspec}

\setsansfont[Ligatures=TeX]{OpenSans-Light}

%\setsansfont[Ligatures=TeX]{Jost-Thin}
%\setmainfont[
%	Ligatures=TeX,
%	BoldFont={Jost-Book},
%	ItalicFont={Jost-LightItalic},
%	BoldItalicFont={Jost-BookItalic}
% ]{Jost-Light}
% also turn off justification with looks not so nice Jost*
%\usepackage[document]{ragged2e}

% for graphics
\usepackage{graphicx}

% ragged left
\usepackage[document]{ragged2e}

% make the pages a little wider
\addtolength{\oddsidemargin}{-.1in}
\addtolength{\evensidemargin}{-.1in}
\addtolength{\textwidth}{0.2in}

%\usepackage[scaled=0.92]{helvet}

\setlength{\parindent}{0pt}
\setlength{\parskip}{1ex plus 0.5ex minus 0.2ex}

% for adding the title page as pdf
\usepackage{pdfpages}

% turn off numbering of sections
\setcounter{secnumdepth}{1}

%reset section numbers with each chapter
\makeatletter
\@addtoreset{chapter}{part}
\makeatother 
% uncomment below to fix /autorefs if necessary
%\renewcommand*{\theHchapter}{\thepart.\thebook.\thechapter}

% macros for TODOs and LATER
\newcommand{\todo}[1]{\textbf{\underline{TODO: {#1}}}\linebreak}
\newcommand{\later}[1]{\textbf{LATER:} {#1}\linebreak}

\setlength{\emergencystretch}{2em} % this remedies a few overfull/underfull hbox warnings

% use fancyhdr for footer (relevant for footnotes?)
\usepackage{fancyhdr}
\fancypagestyle{plain}{%
	\fancyhf{} % clear all header and footer fields
	\fancyfoot[C]{\bfseries \thepage} % except the center
	\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{0pt}
}
\pagestyle{plain}

% define custom date and time formats for document version
%\usepackage{ngerman}
%\usepackage[ngerman]{datetime}
\usepackage{datetime}
\newdateformat{dver}{\twodigit{\THEYEAR}.\twodigit{\THEMONTH}\twodigit{\THEDAY}}
\newtimeformat{tver}{\twodigit{\THEHOUR}\twodigit{\THEMINUTE}} 

% source name and version in left footer 
\lfoot{\fontsize{5}{5}\selectfont \jobname.\dver\today.\tver}
% pagenumber in right footer
\rfoot{\fontsize{8}{8}\selectfont \thepage}

% remove page number from center footer
\cfoot[]{}
